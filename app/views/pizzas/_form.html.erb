<%= form_with(model: pizza) do |form| %>
  <% if pizza.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pizza.errors.count, "error") %> prohibited this pizza from being saved:</h2>

      <ul>
        <% pizza.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <div class="form-fields">
      <%= form.label :name %>
      <%= form.text_field :name %>

      <%# TODO: build out JS for dynamically adding/removing multiple toppings %>
      <%#= form.label :toppings %>
      <%#= form.select :toppings, options_for_select(@toppings) %>
    </div>

    <div class="submit-button-wrapper">
      <%= form.submit %>
    </div>
  </div>
<% end %>
